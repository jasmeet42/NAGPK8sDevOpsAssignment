apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: k8s-namespace
spec:
  type: ClusterIP   # ✅ explicit
  selector:
    app: node-api
  ports:
    - port: 80
      targetPort: 8080